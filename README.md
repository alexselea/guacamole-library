Guacamole Library GO

The project represents a client for the guacamole protocol, written in GO.


Just two commands needed to put the docker compose environment up and running:
`docker-compose build`
`docker-compose up`
